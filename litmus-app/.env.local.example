# Solana RPC endpoint (mainnet). Falls back to public mainnet-beta if unset.
NEXT_PUBLIC_SOLANA_RPC=https://api.mainnet-beta.solana.com

# LMT SPL token mint address. If unset, on-chain wallet balance reads are skipped.
NEXT_PUBLIC_LMT_MINT=
